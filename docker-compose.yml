services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: interior_estimate
      POSTGRES_USER: interior
      POSTGRES_PASSWORD: interior
    ports:
      - "5432:5432"
    volumes:
      - interior_pgdata:/var/lib/postgresql/data

volumes:
  interior_pgdata:
